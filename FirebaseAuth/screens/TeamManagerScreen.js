import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TeamManager = () => {
  return (
    <View>
      <Text>TeamManager</Text>
    </View>
  );
};

export default TeamManager;

const styles = StyleSheet.create({});
